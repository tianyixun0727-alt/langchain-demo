#!/usr/bin/env python3
"""模型上下文协议（MCP）概述"""

print("""
=========================================
模型上下文协议（MCP）概述
=========================================

什么是 MCP？
------------
MCP（Model Context Protocol）是一种开放标准协议，
用于将大语言模型（LLM）与外部工具、数据源和系统连接起来。

你可以把 MCP 理解为：

    👉 AI 应用的“标准化接口协议”
    👉 用统一方式连接所有工具和数据系统

--------------------------------------------------------
MCP 的核心架构
----------------

MCP 采用“客户端-服务端”结构：

    客户端（LangChain / Agent）
                │
                ▼
        MCP 协议层（通信标准）
                │
                ▼
            MCP 服务器
        ├── 工具（Tools）
        ├── 资源（Resources）
        └── 提示词（Prompts）

--------------------------------------------------------
MCP 的三大核心概念
------------------

1️⃣ 工具（Tools）
-----------------
工具是可以被模型调用的函数能力，例如：

- 查询数据库
- 调用API接口
- 执行计算任务
- 搜索信息

👉 本质：可执行函数

--------------------------------------------------------

2️⃣ 资源（Resources）
---------------------
资源是 MCP 服务器提供的数据内容，例如：

- 文件内容
- 数据库记录
- API返回数据
- 文档信息

👉 本质：可读取的数据源

--------------------------------------------------------

3️⃣ 提示词（Prompts）
---------------------
提示词是可复用的标准化 prompt 模板，例如：

- 系统提示词模板
- 固定任务指令
- 行为规范模板

👉 本质：可复用的提示结构

--------------------------------------------------------
为什么需要 MCP？
-----------------

在 MCP 出现之前：

  每个工具都需要单独集成

  Agent A ──自定义代码──→ 数据库
  Agent B ──自定义代码──→ 数据库
  Agent C ──自定义代码──→ 数据库

问题：
- 重复开发
- 难以复用
- 难以扩展

--------------------------------------------------------

使用 MCP 之后：

  所有 Agent 通过统一协议访问工具

  Agent A ─┐
  Agent B ─┼── MCP 协议 ──→ MCP 服务器 ──→ 数据库
  Agent C ─┘

优势：
- 一次集成，多处使用
- 标准统一
- 易扩展

--------------------------------------------------------
MCP 的核心能力
----------------

1️⃣ 动态工具发现
   - 运行时自动获取可用工具
   - 无需写死 tool list

2️⃣ 标准化协议
   - 不同模型统一接口
   - 支持 OpenAI / Anthropic / Google 等

3️⃣ 安全执行机制
   - 工具运行在受控环境
   - 可配置权限访问

4️⃣ 跨框架兼容
   - 可用于 LangChain / LlamaIndex 等框架

--------------------------------------------------------
LangChain 中如何使用 MCP
------------------------

LangChain 可以通过适配器使用 MCP：

示例代码：

    from langchain_mcp_adapters.client import MultiServerMCPClient

    client = MultiServerMCPClient({
        "demo-server": {
            "url": "http://localhost:8000/mcp"
        }
    })

    # 自动获取 MCP 工具
    tools = client.get_tools()

    # 交给 agent 使用
    agent = create_react_agent(llm, tools)

    result = agent.invoke({
        "input": "查询今天的天气"
    })

--------------------------------------------------------
MCP 能做什么？
---------------

✔ 数据库查询（SQL / NoSQL）
✔ 外部API调用（天气 / 股票 / 搜索）
✔ 文件系统操作（读取/写入文件）
✔ 企业系统对接（ERP / CRM）
✔ 数据分析与计算任务

--------------------------------------------------------
MCP 的核心意义
---------------

MCP 的本质不是“工具调用”，而是：

    👉 AI 工具的统一通信标准

类似：

    HTTP → 统一 Web 通信标准
    MCP  → 统一 AI 工具通信标准

--------------------------------------------------------
快速使用流程
-------------

1. 启动 MCP Server
2. 连接 MCP Client
3. 自动获取 tools / resources
4. 传入 LangChain Agent 使用

--------------------------------------------------------
一句话总结
----------

MCP 是一种标准协议，
用于让 AI 模型以统一方式连接外部工具与数据系统。

=========================================
""")